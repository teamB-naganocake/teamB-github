<%= form_with model: cart_item, url: public_cart_items_path(cart_item), method: :post, local:true do |f| %>
  <%= f.hidden_field :item_id, value: item.id %>
  <%= f.select :amount, *[1..10], include_blank: "個数選択" %>
  <%= f.submit "カートに入れる" %>
<% end %>